--- raven/contrib/flask/__init__.py	2012-12-26 20:41:46.000000000 +0000
+++ raven/contrib/flask/__init__.py	2013-08-17 18:01:27.675886374 +0100
@@ -91,7 +91,8 @@
             self.client = make_client(self.client_cls, app, self.dsn)
 
         if self.logging:
-            setup_logging(SentryHandler(self.client))
+            level = self.logging if type(self.logging) is int else 0
+            setup_logging(SentryHandler(self.client, level=level))
 
         got_request_exception.connect(self.handle_exception, sender=app)
 
